<UserControl x:Class="BotLeecherWPF.BotPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mui="http://firstfloorsoftware.com/ModernUI"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="900">
    <StackPanel> 
        
        <Grid Height="90" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="65" />
                <ColumnDefinition Width="Auto" MinWidth="65" />
                <ColumnDefinition Width="Auto" MinWidth="65" />
                <ColumnDefinition Width="Auto" MinWidth="65" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Grid.Row="0" Content="Refresh" Command="{Binding Path=RefreshCommand}" Margin="10,0" />
            <Button Grid.Column="2" Grid.Row="0" Content="Cancel" Command="{Binding Path=CancelCommand}" Margin="10,0"/>
            <Label Content="Filter" Grid.Column="0" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            <TextBox Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" Margin="5,7,5,2" Text="{Binding Path=Filter, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>
        <ScrollViewer HorizontalContentAlignment="Center" HorizontalAlignment="Center" Margin="0,0,0,0" Width="890" MaxHeight="500">
            <DataGrid x:Name="packGrid" ItemsSource="{Binding Packs}" CanUserAddRows="False" HorizontalAlignment="Center">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Dl" Width="SizeToCells" IsReadOnly="True">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Go" Command="{Binding Path=DataContext.DlCommand,RelativeSource={RelativeSource AncestorType={x:Type DataGrid}}}" CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </StackPanel>
</UserControl>
